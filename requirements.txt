# ── Core UI ──────────────────────────────────────────────────────────────────
streamlit>=1.35.0

# ── Browser WebRTC Webcam (works on Streamlit Cloud & all browsers) ──────────
streamlit-webrtc>=0.47.0
av>=12.0.0                  # Video frame decoding required by streamlit-webrtc

# ── Computer Vision & Detection ──────────────────────────────────────────────
ultralytics>=8.0.0          # YOLOv8 + ByteTracker
opencv-python-headless>=4.8.0   # Headless OpenCV (no GUI window needed in Streamlit)

# ── Deep Learning ─────────────────────────────────────────────────────────────
torch>=2.0.0
torchvision>=0.15.0

# ── Data & Image Handling ─────────────────────────────────────────────────────
numpy>=1.24.0
Pillow>=10.0.0
pandas>=2.0.0               # Results table, CSV export

# ── Networking (upgraded to fix urllib3/chardet warnings) ────────────────────
requests>=2.34.2
urllib3>=2.7.0
charset-normalizer>=3.4.7
